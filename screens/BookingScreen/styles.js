import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginTop: 50,
        width: 450,
        height: 450
    },
    timelineCalendar: {
        width: 300,
        height: 400
    }
})

export default styles